id: missing-versions
name: Missing required versions
keywords: ['failed to resolve module', 'versions', 'export default', 'NoSupportedVersion']
pinned: true
content: |-
  ```
  ModuleError: NoVersionsSupported Module script is missing the required (versions: string|string[]) property
  ```
  • Make you defined `versions` in your module
  • Check if you mispelled `versions`
  • Make sure you exported your module using `module.exports` or `export default`